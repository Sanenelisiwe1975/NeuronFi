[build]
builder = "nixpacks"
buildCommand = "npm ci && cd packages/kite && npm run build && cd ../.."

[deploy]
startCommand = "node packages/agent/dist/index.js"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
